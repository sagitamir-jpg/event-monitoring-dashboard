// Simple Event Service (placeholder)
class EventService {
  async getAllEvents(userSettings) {
    // Return empty array for now - this prevents errors
    return []
  }
}

export default new EventService()
